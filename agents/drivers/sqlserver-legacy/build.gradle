dependencies {
    implementation 'com.microsoft.sqlserver:mssql-jdbc:13.2.0.jre11'
}

tasks.named('shadowJar') {
    manifest {
        attributes(
            'Agent-Label': 'SQL Server TLS 1.0 Compatibility Driver',
            'Main-Class': 'com.dbx.agent.sqlserverlegacy.SqlServerLegacyAgent'
        )
    }
}
